---
import Welcome from '../components/Welcome.astro'
import Layout from '../layouts/Framed.astro'

/* Initialize "random" sites. */
// NOTE: Also in `src/components/Footer.astro`
const listRandomSites = [
	'https://designmuseum.dk/',
	'https://steffiedeleeuw.com/',
	'https://www.wikipedia.org/',
	'https://www.pichiavo.com/',
	'https://noma.dk/',
	'https://www.landia.com/los-angeles/',
	'https://www.caesarstoneus.com/',
	'https://www.noguchi.org/',
	'https://quebec.ubisoft.com/en/',
	'https://raycharles.cydstumpel.nl/',
	'https://www.artgalleryofballarat.com.au/',
	'https://tetuhi.art/',
	'https://fabbricagroup.fr/',
]

/* Select a random target. */
const target = listRandomSites[Math.floor(Math.random() * listRandomSites.length)]
---

<Layout>
	<!-- <Welcome /> -->
	<iframe
		id="web-stage"
		src={target}
		class="w-full h-full"
	/>
</Layout>

<script is:inline>
/* Set target. */
const target = location.hash?.slice(1)

/* Validate target. */
if (typeof target !== 'undefined') {
	/* Initialize Iframe reference. */
	const iFrame = globalThis.document.getElementById('web-stage')

	/* Validate target. */
	if (target.includes('https://')) {
		/* Go to target. */
		iFrame.src = target
	} else {
		/* Go to target. */
		iFrame.src = 'https://' + target
	}
}
</script>

<style>
/* iframe::-webkit-scrollbar {
	width: 4px;
	height: 8px;
	background-color: #763a6d;
} */
/* iframe::-webkit-scrollbar-thumb {
	background: #d922be;
} */
</style>
